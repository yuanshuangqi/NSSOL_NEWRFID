package com.nssol.dao.cluster;

import org.apache.ibatis.annotations.Mapper;

import com.nssol.model.PlanningData;

@Mapper
public interface ClusterPlanningInfo {
	
	//PlanningData getSort(PlanningData planningData);

}
